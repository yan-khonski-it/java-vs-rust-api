# rust-app

This is the second application. It is written in Rust with Actix Web (similar to SpringBoot in Java).

Buidl docker image:
```shell
docker build -t rust-app .
```

Run docker container:
```shell
docker run -p 8081:8081 rust-app
```
